# Environment variables for the Trenitalia frontend
# This file contains configuration for local development

# API base URL - points to the Cloudflare Worker backend
# For local development, this should point to your locally running backend
REACT_APP_API_BASE=http://localhost:8787

# For production deployment, this should be set to the deployed worker URL
# REACT_APP_API_BASE=https://your-worker-url.your-subdomain.workers.dev